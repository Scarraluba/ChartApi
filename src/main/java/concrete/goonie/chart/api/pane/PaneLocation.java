package concrete.goonie.chart.api.pane;

/**
 * Vertical placement mode for a chart pane.
 */
public enum PaneLocation {
    /** Renders inside the main chart content area at the bottom. */
    OVERLAY_BOTTOM,
    /** Renders as a dedicated pane stacked vertically under the main chart. */
    STACKED
}
